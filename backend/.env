# Notes
# - PORT → The port your Express server runs on. Default is 5000.
# - MONGO_URI → Connection string for MongoDB. Use local (mongodb://localhost:27017/authSystem) if MongoDB is installed, or Atlas URI if using cloud.
# - JWT_SECRET → Any random string. This is used to sign and verify tokens. Keep it private.

# Server Port
PORT=5000

# MongoDB Connection String
# For local MongoDB:
MONGO_URI=mongodb://localhost:27017/authSystem

# For MongoDB Atlas (cloud):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/authSystem

# JWT Secret Key
JWT_SECRET=your_jwt_secret_key_here